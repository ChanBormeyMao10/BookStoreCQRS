@model Book6.UpdateUser

@{
    ViewBag.Title = "Update a Book";
}

<h2>Update Book</h2>
<fieldset>
    @using (Html.BeginForm())
    {

        <div>
            <label>UserName</label>
            @Html.TextBoxFor(u => u.Name)
        </div>
        <div>
            @Html.HiddenFor(u => u.Id)
        </div>

        <button type="submit" class="btn">Submit</button>

    }
</fieldset>
